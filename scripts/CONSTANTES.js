    const SERVER_NAME = "https://modulo-sorteios.azurewebsites.net/";
    const EMAIL = sessionStorage.getItem("email")
    const TOKEN = sessionStorage.getItem("token");

    export {SERVER_NAME, EMAIL, TOKEN}
